# 阶段5第5批真机反馈改造实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复标签即时写库和历史版本不含标签的问题，并把搜索范围弹窗改造成按需加载、支持名称搜索的书章节树。

**Architecture:** `NoteEditorVm` 持有标签草稿，`LocalNoteRepository` 用单个 Drift 事务保存标题、正文、重点和标签，并通过兼容编解码器写完整历史快照。范围选择器通过独立 autoDispose ViewModel 按需读取根书和子节点，名称搜索返回带祖先的路径。

**Tech Stack:** Flutter 3.44.4、Dart 3.12.2、Riverpod 3.3.2（手写 Provider）、Drift 2.34.2、flutter_quill 11.5.1、go_router 17.3、flutter_test。

## 执行状态（2026-07-28 22:20）

- Task 1—5 已按 RED → GREEN → REFACTOR 完成并分别提交。
- Task 6 的格式化、差异检查、全量 analyze、全量 test、独立代码审查和 Debug APK 构建已完成。
- 最终自动化结果：`flutter analyze --no-pub` 零问题；`flutter test --no-pub` 138 项全部通过；Debug APK SHA-256 为 `E00096341CFA44F695A2DB1830DC1C6195A0016F6772908E3DF491BB8734B299`。
- 独立审查发现的标题清空、保存期间标签草稿、损坏旧快照和搜索祖先合并问题均已关闭；最终复核 Critical 0、Important 0。
- 尚未执行用户真机验收、备份清理和远程 push；这些操作继续等待用户确认。

## Global Constraints

- 当前分支固定为 `feature/notes-mvp`，不 reset、不改写历史、不 push。
- 修改每个现有文件前复制到 `beifen/`，真机验收前不删除本轮备份。
- 不修改数据库表和 schemaVersion，不运行 Drift 代码生成。
- 新建笔记仅有标签时仍视为空笔记，不创建笔记和标签。
- 标签名称 trim 后去重，英文大小写沿用现有规则。
- 新历史快照保存标题、正文、标签；旧正文 Delta 只恢复正文。
- 未输入科目关键词时只显示顶层书；书展开章，章展开节。
- 多标签并集、关键词匹配、范围筛选的既有语义不得改变。
- 不处理 F1.1.11、F1.1.12、F1.1.15。
- 所有生产代码必须先有失败测试并确认失败原因正确。

---

## 文件职责

### 新建文件

- `lib/src/features/notes/models/note_edit_snapshot.dart`：完整历史快照的规范化、编码和新旧格式解析。
- `lib/src/features/notes/models/subject_path.dart`：根书到匹配节点的不可变路径。
- `lib/src/features/notes/view_model/subject_scope_picker_view_model.dart`：根书、子节点缓存、搜索路径、展开状态和错误状态。
- `test/features/notes/note_edit_snapshot_test.dart`：快照格式和兼容性单元测试。
- `test/features/notes/subject_scope_picker_view_model_test.dart`：范围树状态、防抖、缓存和竞态测试。
- `test/features/notes/subject_scope_picker_test.dart`：范围树 Widget 行为测试。

### 修改文件

- `lib/src/features/notes/repository/note_repository.dart`：保存接口增加标签名称参数。
- `lib/src/features/notes/repository/local_note_repository.dart`：完整状态比较、原子保存、完整恢复和旧版恢复。
- `lib/src/features/notes/view_model/note_editor_view_model.dart`：持有标签草稿并参与 dirty/save。
- `lib/src/features/notes/view/note_editor_view.dart`：移除标签即时写库，改用 ViewModel 草稿。
- `lib/src/data/database/daos/subject_dao.dart`：名称字面量搜索。
- `lib/src/features/notes/repository/subject_repository.dart`：增加 `searchPaths`。
- `lib/src/features/notes/repository/local_subject_repository.dart`：补齐祖先路径。
- `lib/src/features/notes/view_model/view_model_providers.dart`：注册范围选择器会话 Provider。
- `lib/src/features/notes/view/subject_scope_picker.dart`：按需树、搜索框和全局范围入口。
- `lib/src/features/notes/view/note_search_view.dart`：不再传入全量科目。
- `lib/src/features/notes/view/tag_management_view.dart`：不再传入全量科目。
- 相关测试中的 `NoteRepository`、`SubjectRepository` fake：同步新增接口参数和方法。

---

### Task 1: 完整历史快照编解码器

**Files:**
- Create: `lib/src/features/notes/models/note_edit_snapshot.dart`
- Create: `test/features/notes/note_edit_snapshot_test.dart`

**Interfaces:**
- Produces: `NoteEditSnapshot({String? title, String? contentJson, Iterable<String> tagNames, bool isLegacy = false})`
- Produces: `String encode()`
- Produces: `static NoteEditSnapshot decode(String snapshotJson)`
- Produces: `bool hasSameContent(NoteEditSnapshot other)`

- [ ] **Step 1: 写新格式往返失败测试**

测试手工构造标题、正文和包含空格/重复项的标签，断言 `decode(encode())` 后标题正文不变，标签为 `['重点', '复习']`。

- [ ] **Step 2: 写旧格式兼容失败测试**

传入顶层 JSON 数组 `[{\"insert\":\"旧正文\\n\"}]`，断言 `isLegacy == true`、`contentJson` 为原字符串、标题为空、标签为空。

- [ ] **Step 3: 运行 RED**

Run:

```powershell
flutter test --no-pub test/features/notes/note_edit_snapshot_test.dart
```

Expected: FAIL，原因是 `note_edit_snapshot.dart` 或 `NoteEditSnapshot` 尚不存在。

- [ ] **Step 4: 实现最小模型**

新格式顶层必须是对象且 `schemaVersion == 1`；旧格式仅接受顶层数组；其他数据抛 `FormatException`。标签按 trim、去空、保持首次出现顺序去重。

- [ ] **Step 5: 运行 GREEN**

Run:

```powershell
flutter test --no-pub test/features/notes/note_edit_snapshot_test.dart
```

Expected: PASS。

- [ ] **Step 6: 本地提交**

```powershell
git add lib/src/features/notes/models/note_edit_snapshot.dart test/features/notes/note_edit_snapshot_test.dart
git commit -m "feat: 新增完整笔记历史快照格式"
```

---

### Task 2: Repository 原子保存与完整历史恢复

**Files:**
- Modify: `lib/src/features/notes/repository/note_repository.dart`
- Modify: `lib/src/features/notes/repository/local_note_repository.dart`
- Modify: `test/features/notes/local_note_repository_test.dart`
- Modify: all test fakes implementing `NoteRepository`

**Interfaces:**
- `create(..., Iterable<String> tagNames = const [])`
- `update(..., Iterable<String>? tagNames)`
- `tagNames == null` 表示保持标签不变；非 null 表示以规范化结果整体替换。

- [ ] **Step 1: 备份 Task 2 现有文件**

把 Repository、Repository 测试和所有受接口变化影响的 fake 所在文件复制到 `beifen/`，文件名追加 `.backup-20260728-before-full-snapshot`。

- [ ] **Step 2: 写标签-only 历史版本失败测试**

用内存数据库创建带 `旧标签` 的笔记，再调用 `update(tagNames: ['新标签'])`。断言：

- 当前关联只有 `新标签`；
- 历史版本恰好一条；
- 解码历史快照后标签为 `旧标签`，标题和正文也是旧值。

- [ ] **Step 3: 写完整恢复失败测试**

先把标题、正文和标签从 A 改成 B，再恢复版本 1，断言标题、正文、纯文本、重点记录和标签全部恢复 A，并额外保存恢复前的 B 快照。

- [ ] **Step 4: 写旧快照恢复失败测试**

直接插入旧正文 Delta 版本，恢复后断言正文变化，但当前标题和标签保持不变。

- [ ] **Step 5: 运行 RED**

```powershell
flutter test --no-pub test/features/notes/local_note_repository_test.dart
```

Expected: FAIL，原因是保存接口不接受 `tagNames`，历史快照也不能解析为完整状态。

- [ ] **Step 6: 扩展接口并同步测试 fake**

为 `create` 增加默认空标签，为 `update` 增加可空标签；测试 fake 必须接受参数但只能在对应行为测试中记录或使用它，不能用 fake 自身断言代替真实 Repository 测试。

- [ ] **Step 7: 实现原子保存**

在 `LocalNoteRepository` 的现有事务内读取旧标签名称，比较规范化后的完整状态；有变化才写 `NoteEditSnapshot`。随后更新 note/highlight，并在传入 `tagNames` 时按名称复用或创建标签、整体替换 `note_tag`。

- [ ] **Step 8: 实现新旧格式恢复**

解析目标快照；旧格式把当前标题和标签合并进目标；恢复前写当前完整快照；随后在同一事务恢复 note/highlight/note_tag。

- [ ] **Step 9: 运行 GREEN 与接口回归**

```powershell
flutter test --no-pub test/features/notes/local_note_repository_test.dart
flutter test --no-pub test/features/notes/note_editor_view_model_test.dart test/features/notes/note_version_view_model_test.dart test/features/notes/note_search_view_model_test.dart test/features/notes/tag_search_view_model_test.dart
```

Expected: 全部 PASS。

- [ ] **Step 10: 本地提交**

```powershell
git add lib/src/features/notes/repository lib/src/features/notes/models test/features/notes
git commit -m "feat: 原子保存笔记标签与完整历史版本"
```

---

### Task 3: 编辑器标签草稿和退出语义

**Files:**
- Modify: `lib/src/features/notes/view_model/note_editor_view_model.dart`
- Modify: `lib/src/features/notes/view/note_editor_view.dart`
- Modify: `test/features/notes/note_editor_view_model_test.dart`
- Modify: `test/features/notes/note_editor_tag_test.dart`
- Modify: `test/features/notes/note_editor_history_entry_test.dart`
- Modify: `test/features/notes/note_editor_stability_test.dart`

**Interfaces:**
- `NoteEditorState.tagNames: List<String>`
- `NoteEditorVm.addTagName(String name)`
- `NoteEditorVm.removeTagName(String name)`
- `NoteEditorVm.save(...)` 从当前状态读取 `tagNames` 并传给 Repository。

- [ ] **Step 1: 备份 Task 3 文件**

复制上述现有文件到 `beifen/`，后缀 `.backup-20260728-before-tag-draft`。

- [ ] **Step 2: 写 ViewModel 标签草稿 RED**

断言初始化已有笔记时读取关联标签；添加/移除只更新 `NoteEditorState.tagNames` 和 `dirty`，不会调用任何标签写接口；保存时 `NoteRepository.update` 收到最终标签名称。

- [ ] **Step 3: 写 Widget 退出 RED**

使用真实内存数据库复现：

1. 添加标签后点返回；
2. 选择“不保存离开”；
3. 重新打开；
4. 断言新增标签不存在。

为移除标签重复同一流程，断言原标签仍存在。

- [ ] **Step 4: 写标签-only 保存与历史入口 RED**

只改标签后点击保存，断言数据库标签更新且产生历史版本；只改标签后打开历史，必须先保存再进入版本页。

- [ ] **Step 5: 运行 RED**

```powershell
flutter test --no-pub test/features/notes/note_editor_view_model_test.dart test/features/notes/note_editor_tag_test.dart test/features/notes/note_editor_history_entry_test.dart
```

Expected: FAIL，原因是已有标签仍即时写库且标签不属于 `NoteEditorState`。

- [ ] **Step 6: 实现 ViewModel 草稿**

初始化已有笔记时并行读取 Note 与标签；在当前 init generation 内一次发布状态。标签操作只修改规范化名称列表并调用 `markDirty` 等价逻辑。

- [ ] **Step 7: 简化编辑器标签区**

删除 `_pendingTagNames`、即时 `addByName/removeTag`、标签操作 generation 和单独 flush。标签 Chip 直接来自 `NoteEditorState.tagNames`，新增/删除调用 ViewModel；建议目录仍来自 `tagManagementVmProvider`。

- [ ] **Step 8: 统一保存和返回**

`_save` 只调用一次 ViewModel 保存；`_back` 和 `_openHistory` 仅依赖统一 `dirty`。保存失败保留页面和草稿。

- [ ] **Step 9: 运行 GREEN 与编辑器回归**

```powershell
flutter test --no-pub test/features/notes/note_editor_view_model_test.dart test/features/notes/note_editor_tag_test.dart test/features/notes/note_editor_history_entry_test.dart test/features/notes/note_editor_stability_test.dart
```

Expected: 全部 PASS。

- [ ] **Step 10: 本地提交**

```powershell
git add lib/src/features/notes/view_model/note_editor_view_model.dart lib/src/features/notes/view/note_editor_view.dart test/features/notes
git commit -m "fix: 统一笔记标签草稿与退出保存语义"
```

---

### Task 4: 科目名称搜索和祖先路径

**Files:**
- Create: `lib/src/features/notes/models/subject_path.dart`
- Modify: `lib/src/data/database/daos/subject_dao.dart`
- Modify: `lib/src/features/notes/repository/subject_repository.dart`
- Modify: `lib/src/features/notes/repository/local_subject_repository.dart`
- Create or Modify: `test/features/notes/local_subject_repository_test.dart`
- Modify: test fakes implementing `SubjectRepository`

**Interfaces:**
- `SubjectPath(List<Subject> nodes)`，要求非空且按根到目标排列。
- `SubjectRepository.searchPaths(String keyword)`
- 空白关键词返回空列表。

- [ ] **Step 1: 备份 Task 4 文件**

复制现有 DAO、Repository 和受接口影响的 fake 文件到 `beifen/`，后缀 `.backup-20260728-before-subject-search`。

- [ ] **Step 2: 写 Repository 搜索路径 RED**

构造两本书、同名章和节，断言搜索章返回两条不同书路径，搜索节返回完整三节点路径，软删除节点不出现。

- [ ] **Step 3: 写 LIKE 字面量 RED**

创建名称含 `%`、`_`、反斜杠的节点，断言只匹配包含对应普通字符的名称。

- [ ] **Step 4: 运行 RED**

```powershell
flutter test --no-pub test/features/notes/local_subject_repository_test.dart
```

Expected: FAIL，原因是 `searchPaths` 尚不存在。

- [ ] **Step 5: 实现 DAO 和 Repository**

DAO 使用与标签搜索相同顺序转义 `\`、`%`、`_`；Repository 对每个匹配节点最多读取两级父节点，父节点缺失或软删除时丢弃无效路径。

- [ ] **Step 6: 运行 GREEN**

```powershell
flutter test --no-pub test/features/notes/local_subject_repository_test.dart
```

Expected: PASS。

- [ ] **Step 7: 本地提交**

```powershell
git add lib/src/data/database/daos/subject_dao.dart lib/src/features/notes/models/subject_path.dart lib/src/features/notes/repository test/features/notes
git commit -m "feat: 支持书章节名称与路径搜索"
```

---

### Task 5: 可搜索的按需范围树

**Files:**
- Create: `lib/src/features/notes/view_model/subject_scope_picker_view_model.dart`
- Modify: `lib/src/features/notes/view_model/view_model_providers.dart`
- Modify: `lib/src/features/notes/view/subject_scope_picker.dart`
- Modify: `lib/src/features/notes/view/note_search_view.dart`
- Modify: `lib/src/features/notes/view/tag_management_view.dart`
- Create: `test/features/notes/subject_scope_picker_view_model_test.dart`
- Create: `test/features/notes/subject_scope_picker_test.dart`
- Modify: `test/features/notes/note_search_view_test.dart`
- Modify: `test/features/notes/tag_management_view_test.dart`

**Interfaces:**
- `showSubjectScopePicker({required BuildContext context})`
- `subjectScopePickerVmProvider(Object sessionKey)`
- `loadChildren(String parentId)`, `toggleExpanded(String id)`, `setKeyword(String value)`, `retryRoot()`, `retrySearch()`

- [ ] **Step 1: 备份 Task 5 文件**

复制所有现有修改文件到 `beifen/`，后缀 `.backup-20260728-before-scope-tree`。

- [ ] **Step 2: 写 ViewModel RED**

覆盖：

- build 只调用 `childrenOf(null)`；
- 第一次展开读取直接子节点，第二次展开使用缓存；
- 250ms 内多次输入只搜索最后关键词；
- 慢旧请求不能覆盖快新请求；
- 搜索失败保留关键词；
- 单节点加载失败只记录该节点错误。

- [ ] **Step 3: 运行 ViewModel RED**

```powershell
flutter test --no-pub test/features/notes/subject_scope_picker_view_model_test.dart
```

Expected: FAIL，原因是 Provider、状态和方法尚不存在。

- [ ] **Step 4: 实现最小 ViewModel**

Timer 在 `ref.onDispose` 中取消；每次搜索递增 generation；所有异步返回先检查 `ref.mounted` 和 generation。子节点缓存按 parentId 保存不可变列表。

- [ ] **Step 5: 写 Widget RED**

覆盖：

- 默认显示书、不显示章节；
- 点击展开图标显示章，再展开显示节；
- 点击展开不关闭弹窗；
- 点击名称返回正确 `SubjectScope.subtree`；
- 搜索章显示所属书，搜索节显示完整路径；
- 清空搜索恢复根书；
- 关闭重开清空搜索和展开状态；
- 全局四种范围可直接选择。

- [ ] **Step 6: 运行 Widget RED**

```powershell
flutter test --no-pub test/features/notes/subject_scope_picker_test.dart
```

Expected: FAIL，原因是现有弹窗仍要求全量 `subjects` 并扁平渲染。

- [ ] **Step 7: 实现范围树界面**

使用 `TextField`、`ListTile`、缩进和展开图标构造 Material 树，不引入第三方依赖。搜索结果树合并重复祖先；搜索匹配路径保持父节点可见。

- [ ] **Step 8: 接入两个搜索页**

删除对 `subjectManageVmProvider` 全量科目的读取和 `subjects` 参数；选择结果后继续调用现有 `setSubjectScope`。

- [ ] **Step 9: 运行 GREEN 与页面回归**

```powershell
flutter test --no-pub test/features/notes/subject_scope_picker_view_model_test.dart test/features/notes/subject_scope_picker_test.dart test/features/notes/note_search_view_test.dart test/features/notes/tag_management_view_test.dart
```

Expected: 全部 PASS。

- [ ] **Step 10: 本地提交**

```powershell
git add lib/src/features/notes/view_model lib/src/features/notes/view test/features/notes
git commit -m "feat: 新增可搜索的书章节范围树"
```

---

### Task 6: 全量验证、审查与真机包

**Files:**
- Modify: `jihua/ruoke-阶段5第5批真机反馈改造实施计划-20260728.md`（更新执行状态）
- Modify after explicit existing authorization: `lujing.md`
- Modify: `.ai-context/TASK_HANDOFF.md`

- [ ] **Step 1: 格式化本轮 Dart 文件**

```powershell
dart format lib/src/features/notes lib/src/data/database/daos test/features/notes
```

- [ ] **Step 2: 差异检查**

```powershell
git diff --check
git status --short
git diff --stat
```

- [ ] **Step 3: 静态检查**

```powershell
flutter analyze --no-pub
```

Expected: `No issues found!`

- [ ] **Step 4: 全量测试**

```powershell
flutter test --no-pub
```

Expected: 全部测试通过；已知 Drift 多数据库警告只允许作为既有测试警告，不得有失败。

- [ ] **Step 5: 独立代码审查**

重点检查事务原子性、旧快照兼容、异步 generation、autoDispose 生命周期和退出语义。Critical/Important 必须关闭后才能构建 APK。

- [ ] **Step 6: Debug APK**

```powershell
flutter build apk --debug --no-pub
Get-FileHash -Algorithm SHA256 build\app\outputs\flutter-apk\app-debug.apk
```

- [ ] **Step 7: 更新文档**

把实际方案、实施步骤、文件变化、问题和自动化验证追加到 `lujing.md`，并用固定九节结构更新 `.ai-context/TASK_HANDOFF.md`。不得把未进行的真机验证写成通过。

- [ ] **Step 8: 交付真机清单**

至少包含：添加标签不保存、移除标签不保存、标签-only 保存与历史、完整恢复、旧版本恢复、默认根书、逐级展开、书章节名称搜索、全局层级范围、会话重置。

- [ ] **Step 9: 等待用户真机验收**

不删除本轮备份、不 push。用户确认通过后再单独决定备份清理和远程推送。
